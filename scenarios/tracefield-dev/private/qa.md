# QA 私的知見（QA のみ）
- 既存 55 テストは Reference を毎回 start_link する前提（インメモリ・独立）。永続化が既定 ON だとテスト間汚染が起きる。
- tmp ディレクトリ注入（memory_dir 方式の前例）がテスト分離に有効だった。
- 復元ロジック（ログ→状態）は property test 的に「保存→復元→同一」を検証すべき。
- 破損ファイル（途中で切れた JSONL 行）への耐性テストが必要。
