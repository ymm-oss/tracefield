# 依頼: `merge_intervals` の徹底的なテストスイートを書け

`inputs/spec.md` の仕様を持つ関数 `merge_intervals(intervals)` に対する、実行可能な **pytest** テストスイートを作成せよ。

- テストは `from target import merge_intervals` で関数をインポートすること。
- 各テストは具体的な入力と期待出力を `assert` で検証する（個別の `test_*` 関数として書く）。
- 仕様で保証されるべき挙動を**漏れなく**カバーすることを最優先とする。
- 実装は与えられない。**仕様だけ**からテストを書け（実装に過適合させない）。

日本語の説明は不要。**最終出力は単一の ```python コードブロックのみ**（そのまま `pytest` で走る形）。
