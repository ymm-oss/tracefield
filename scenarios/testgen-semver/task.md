# 依頼: `compare_versions` の徹底的なテストスイートを書け

`inputs/spec.md` の仕様を持つ関数 `compare_versions(a, b)`（セマンティックバージョン比較、戻り値 `-1/0/1`）に対する、実行可能な **pytest** テストスイートを作成せよ。

- テストは `from target import compare_versions` で関数をインポートすること。
- 各テストは具体的な入力と期待出力を `assert` で検証する（個別の `test_*` 関数として書く）。
- 仕様で保証されるべき優先順位規則を**漏れなく**カバーすることを最優先とする。
- 実装は与えられない。**仕様だけ**からテストを書け（実装に過適合させない）。

**最終出力は単一の ```python コードブロックのみ。前置き・説明・散文は一切書くな。最初の行は `from` か `import` で始めよ。**
