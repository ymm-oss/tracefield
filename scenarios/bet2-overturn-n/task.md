# 依頼: 電子カルテ ClinicChart の「記録一覧の日付範囲フィルタ」を設計せよ

ClinicChart（架空の電子カルテ）の記録一覧画面に、表示する記録を**日付範囲で絞り込む**機能を追加する。
実装可能な設計を示せ。設計には少なくとも以下を含めよ:

- **データ取得**（範囲指定をどうクエリするか）
- **UI**（範囲の指定方法と一覧の見え方）
- **権限**（誰が何を見られるか）
- **既存画面への影響**

詳細な製品コンテキストは `inputs/brief.md` を読め。具体・因果・実装可能性を要求する。日本語で書け。
